<Window x:Class="Feleves_EC211O.ResultsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Feleves_EC211O"
        mc:Ignorable="d"
        Background="#0D0D0D" Foreground="White" FontFamily="Consolas"
        Title="ResultsWindow" Height="450" Width="800">
    <Grid Margin="20">

        <TextBlock Text="Eredménytábla"
                   FontSize="26"
                   FontWeight="Bold"
                   Foreground="#E10600"
                   HorizontalAlignment="Center"
                   Margin="0,0,0,15"/>

        <DataGrid x:Name="ResultGrid"
          AutoGenerateColumns="False"
          IsReadOnly="True"
          CanUserAddRows="False"
          HeadersVisibility="Column"
          RowHeight="35"
          ColumnHeaderHeight="35"
          AlternationCount="2"
          Margin="0,50,0,60"
          Background="#1E1E1E"
          Foreground="White"
          BorderBrush="#E10600"
          BorderThickness="2"
          FontSize="14"
          HorizontalGridLinesBrush="#E10600"
          VerticalGridLinesBrush="#E10600"
          AlternatingRowBackground="#151515"
          RowHeaderWidth="0"
          SelectionMode="Single"
          SelectionUnit="FullRow">

            <DataGrid.RowStyle>
                <Style TargetType="DataGridRow">
                    <Setter Property="Background" Value="#1E1E1E"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="BorderBrush" Value="#0D0D0D"/>
                    <Setter Property="FontWeight" Value="Normal"/>
                    <Style.Triggers>

                        <DataTrigger Binding="{Binding RelativeSource={RelativeSource Self}, Path=AlternationIndex}" Value="0">
                            <Setter Property="Background" Value="Gold"/>
                            <Setter Property="Foreground" Value="Black"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                        </DataTrigger>

                        <!-- Hover effekt: sor fölé visszük az egeret -->
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#E10600"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>

                        <Trigger Property="IsSelected" Value="True">
                            <Setter Property="Background" Value="#E10600"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </DataGrid.RowStyle>

            <DataGrid.ColumnHeaderStyle>
                <Style TargetType="DataGridColumnHeader">
                    <Setter Property="Background" Value="#E10600"/>
                    <Setter Property="Foreground" Value="White"/>
                    <Setter Property="FontWeight" Value="Bold"/>
                    <Setter Property="BorderBrush" Value="#0D0D0D"/>
                    <Setter Property="FontSize" Value="14"/>
                    <Setter Property="HorizontalContentAlignment" Value="Center"/>
                </Style>
            </DataGrid.ColumnHeaderStyle>

            <DataGrid.Columns>
                <DataGridTextColumn Header="Versenyző" Binding="{Binding Driver}" Width="*" />
                <DataGridTextColumn Header="Legjobb kör" Binding="{Binding BestLap, StringFormat='{}{0:hh\\:mm\\:ss}'}" Width="150"/>
                <DataGridTextColumn Header="Átlag" Binding="{Binding Average, StringFormat='{}{0:hh\\:mm\\:ss}'}" Width="150"/>
                <DataGridTextColumn Header="Körök" Binding="{Binding LapCount}" Width="100"/>
            </DataGrid.Columns>
        </DataGrid>

        <Button Content="Bezárás"
                Width="120"
                Height="35"
                HorizontalAlignment="Right"
                VerticalAlignment="Bottom"
                Margin="0,0,10,10"
                Background="#1E1E1E"
                Foreground="White"
                BorderBrush="#E10600"
                BorderThickness="2"
                FontWeight="Bold"
                Click="Close_Click">
            <Button.Style>
                <Style TargetType="Button">
                    <Setter Property="Template">
                        <Setter.Value>
                            <ControlTemplate TargetType="Button">
                                <Border Background="{TemplateBinding Background}"
                                        BorderBrush="{TemplateBinding BorderBrush}"
                                        BorderThickness="{TemplateBinding BorderThickness}"
                                        CornerRadius="5">
                                    <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                                </Border>
                            </ControlTemplate>
                        </Setter.Value>
                    </Setter>
                    <Style.Triggers>
                        <Trigger Property="IsMouseOver" Value="True">
                            <Setter Property="Background" Value="#E10600"/>
                            <Setter Property="Foreground" Value="White"/>
                        </Trigger>
                    </Style.Triggers>
                </Style>
            </Button.Style>
        </Button>
    </Grid>
</Window>
